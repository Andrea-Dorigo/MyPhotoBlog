window.Rails = Rails;
Rails.$('.gallery_wrapper')[0].innerHTML = ("<%= j (render partial: 'photo_gallery', locals: {
          selected: @selected,
          words_array: @words_array,
          photourl: @photourl
        } ) %>");
document.getElementById('comments_list').innerHTML = ("<%= j (render partial:
 'comments_list', locals: {
   comments: @comments,
   selected: @selected,
   words_array: @words_array
        } ) %>");
document.getElementById("hidden_word").setAttribute("value", "<%= @words_array[@selected] %>");
document.getElementById("refresh_icon").classList.remove("rotating");
history.pushState("data","title"," <%= home_url + "?s=#{@selected}&w=#{@words_string}" %> ");
