

<%= j ( puts "selected: #{params['selected'].to_i}") %>
window.Rails = Rails;
Rails.$('.test')[0].innerHTML = ("<%= j (render partial: 'photo_gallery', locals: {
          selected: params['selected'].to_i || 1,
          words_array: session[:words_array],
          photourl: session[:photourl]
        } ) %>");
