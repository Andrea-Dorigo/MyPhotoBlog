window.Rails = Rails;
Rails.$('.test')[0].innerHTML = ("<%= j (render partial: 'photo_gallery', locals: {
          selected: @selected,
          words_array: @words_array,
          photourl: @photourl
        } ) %>");
